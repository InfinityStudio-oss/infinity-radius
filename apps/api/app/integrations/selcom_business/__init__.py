"""Selcom Business API (developer.selcom.business) — RSA-SHA256 signed
disbursement transport. Distinct from app.integrations.selcom (the older,
still-unimplemented Collection API module) — these are two different
Selcom products with different auth schemes; do not merge them.
"""
